#include <cstdlib>
using namespace std;

int main(int argc, char *argv[])
{
    // Makes a call to the system which is essentially the same as typing the command
    // in at the command line. This stdlib library is a c library, so it is prefixed with
    // "c".
    system("./hello");
    return 0;
}
